---
# Configuring a single instance oracle database server

# Create Oracle user
- name: deploy_oracle_user
  hosts: "{{ targetgroup }}"
  become: yes
  become_user: root
  become_method: sudo
  roles:
   - role: deploy_oracle_user

